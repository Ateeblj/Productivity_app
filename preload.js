// preload.js
// This file runs in the renderer process (web app) and exposes safe APIs.
// Right now it does nothing extra, but it's required for Electron security.
// You can add custom bridges here later if needed.